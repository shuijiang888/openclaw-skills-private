# 协作状态（机器可读字段在下方）

<!-- 
  OpenClaw / Cursor 共用；Cursor 执行任务时更新 YAML 块。
-->
```yaml
task_id: collab-001
state: in_progress
updated: "2026-04-06"
owner: cursor
last_step: "按 HANDOFF 创建 playbook 并跑 check-collab.sh"
blocker: ""
```

## 人类可读备注

首轮演练：从 `in_progress` 到 `done`。
