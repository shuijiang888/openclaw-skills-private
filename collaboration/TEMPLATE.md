# 可复用任务说明模板（OpenClaw → Cursor）

> 用法：复制本文件为当轮草稿，或把各节内容分别写入 `collaboration/CURRENT_TASK.md` 与 `collaboration/HANDOFF.md`（以仓库约定为准）。

## 任务 ID

`task_id: collab-XXX`

## 背景（为什么做这件事）

- 业务/技术背景：
- 依赖前置（已合并的 PR、环境）：

## 完成定义（DoD）

每条须可判定 **通过 / 不通过**（含路径、命令或可见结果）。

- [ ] 条目 1：例如某文件存在且含约定章节
- [ ] 条目 2：例如 `./scripts/xxx.sh` 退出码 0
- [ ] 条目 3：例如 `STATUS.md` 中 `task_id` 与 `state: done` 一致

## 验收步骤

1. **操作**：例如运行 `./scripts/check-collab.sh`
2. **预期**：退出码 0，终端输出含约定成功标记
3. **操作**：例如人工核对 `HANDOFF` 所列路径
4. **预期**：内容与 DoD 一一对应

## 非目标（明确边界）

- 不修改的范围（目录/配置/依赖安装）：
- 不执行的高危或环境变更命令（若有）：

## 安全约束（如果有特殊限制）

- 除 `AGENTS.md` 通用红线外，本轮额外禁止或需先决策的事项：
