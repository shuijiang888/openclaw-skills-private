# 当前任务（OpenClaw 维护）

## 任务 ID

`collab-008`

## 背景

collab-007 H5 已验收。本轮升级为专业版：企业基本信息 4 题、纯 SVG 六维雷达图、html2pdf 真实导出、CONFIG 预约链接与热线可配置、报告版式升级。

## 完成定义（DoD）

见 `collaboration/HANDOFF.md`：`h5_questionnaire.html` 增强，30 步（QB1–QB4 + Q1–Q26），`check-collab.sh` 绑定 collab-008。

## 验收

小江验收 **交互、雷达图、PDF、移动端**；PDF 依赖 CDN 需联网环境。

## 非目标

见 `HANDOFF.md`（无后端、无 CRM、不改 questionnaire_design.md）。
